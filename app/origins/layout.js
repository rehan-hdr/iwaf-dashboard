import PageLayout from '@/components/PageLayout';

export const metadata = { title: 'Origins — IWAF' };

export default function OriginsLayout({ children }) {
  return <PageLayout>{children}</PageLayout>;
}
